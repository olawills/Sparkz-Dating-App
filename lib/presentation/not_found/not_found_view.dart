part of 'not_found_controller.dart';

class _NotFoundView
    extends StatelessView<NotFoundScreen, NotFoundScreenController> {
  const _NotFoundView(NotFoundScreenController state, {Key? key})
      : super(state, key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
