part of '../profile_barrel.dart';

class _ProfileView extends StatelessView<ProfileScreen, ProfileController> {
  const _ProfileView(ProfileController state, {Key? key})
      : super(state, key: key);

  Widget build(BuildContext context) {
    return Scaffold();
  }
}
