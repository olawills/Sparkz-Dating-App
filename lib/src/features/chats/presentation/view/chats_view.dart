part of '../chats_barrel.dart';

class _ChatsView extends StatelessView<ChatsScreen, ChatsController> {
  const _ChatsView(ChatsController state, {Key? key}) : super(state, key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
